version: v1.0
name: Testing
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
global_job_config:
  epilogue:
    always:
      commands:
        - '[[ -f report.xml ]] && test-results publish report.xml'
        - name: 'Block #2'
  global_job_config:
    epilogue: null
    always:
      commands:
        - '[[ -f report.xml ]] && test-results publish report.xml'
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: Unit test
          commands:
            - echo 'running unit tests'
